# Local development environment dependencies.
#
# This is the single source of truth for the project .venv.
# plugins/devenv/cli.py reads this file during `setup`.
#
# Note: torch is installed separately from a CUDA-specific index
# (e.g. cu128, cu130). It is NOT listed here.

# Core service
fastapi>=0.116,<1.0
uvicorn[standard]>=0.35,<1.0
pydantic>=2.0,<3.0
httpx>=0.28,<1.0
psutil>=7.0,<8.0
pyyaml>=6.0,<7.0

# CUDA / GPU
nvidia-cutlass-dsl>=4.0
numpy>=2.0
cuda-python>=13.0

# FA4 CuTe DSL — pure Python JIT, no C++ compilation needed
flash-attn-4>=4.0.0b5
einops>=0.8

# Build tools
ninja>=1.0
packaging>=24.0
wheel>=0.44

# Configuration (for Hydra conf loading)
hydra-core>=1.3,<2.0
omegaconf>=2.3,<3.0

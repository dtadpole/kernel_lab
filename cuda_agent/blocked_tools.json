{
  "description": "CUDA Toolkit binaries that must not be called directly from Bash. All CUDA operations should go through the cuda MCP tools instead.",
  "blocked": [
    "nvcc",
    "ptxas",
    "cuobjdump",
    "nvdisasm",
    "ncu",
    "nvprof",
    "nsys",
    "cuda-gdb",
    "cuda-memcheck",
    "compute-sanitizer"
  ]
}

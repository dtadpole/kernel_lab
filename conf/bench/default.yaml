# Formal benchmark configuration
kernel: ???         # required: fa4, matmul, vecadd
arch: ???           # required: sm90, sm120 (or "auto" for auto-detect)
impls: all          # "all" or list: [ref-cublas, gen-cuda]
timeout: 300        # per-config timeout in seconds
